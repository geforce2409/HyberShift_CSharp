<Window x:Class="HyberShift_CSharp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HyberShift_CSharp"
        xmlns:viewmodel="clr-namespace:HyberShift_CSharp.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <viewmodel:LoginViewModel x:Key="loginViewModel"/>
    </Window.Resources>
    <Grid>
        <!--Test-->
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <Label>Email</Label>
                <TextBox x:Name="txtEmail" Width="200" Text="{Binding TxtEmail, Source={StaticResource loginViewModel}}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label>Password</Label>
                <TextBox x:Name="txtPassword" Width="200" Text="{Binding TxtPassword,Source={StaticResource loginViewModel}}"/>
            </StackPanel>

            <Button x:Name="btnLogin" Command="{Binding Command, Source={StaticResource loginViewModel}}">Login</Button>
        </StackPanel>
        
    </Grid>
</Window>
