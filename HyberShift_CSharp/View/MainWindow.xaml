<Window x:Class="HyberShift_CSharp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HyberShift_CSharp"
        xmlns:viewmodel="clr-namespace:HyberShift_CSharp.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <viewmodel:LoginViewModel x:Key="LoginViewModel" />
    </Window.Resources>
    <Grid>
        <!--Test-->
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <Label>Email</Label>
                <TextBox x:Name="TxtEmail" Width="200"
                         Text="{Binding Email, Source={StaticResource LoginViewModel}}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label>Password</Label>
                <TextBox x:Name="TxtPassword" Width="200"
                         Text="{Binding TxtPassword,Source={StaticResource LoginViewModel}}" />
            </StackPanel>

            <Button x:Name="BtnLogin" Command="{Binding Command, Source={StaticResource LoginViewModel}}">Login</Button>
        </StackPanel>

    </Grid>
</Window>